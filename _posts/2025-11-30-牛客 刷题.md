
https://www.nowcoder.com/exam/oj/ta?tpId=37

1.  while(cin>> input)，能读到带空格输入到最后一个，可以用这种方法处理多个case
2. cin.getline(),能读一行，并且拿走\n. 也可以用get，但是get读行的话\n会留在那里，需要额外调用get
	小写字母比大写字母大32
	cctype里面有个isdigit，能判断数字，还有isupper，能判断大小写
3. 这里注意用数组的下标进行去重加排序操作。最后输出的迭代次数是以数组长度，而不是输入长度来的。 去重加排序
4. 字符串去掉空格。
	getline得到的字符是没有\n的，所以要用\0来判断
5. C++ char转数字 a - 48。十六进制的直接s[i] - 65 + 10就行c++ include cmath 可以使用pow
6. 分解质因子，注意最后的判断，那个质数可能出不来，要专门处理
7. 一般C++可以用map来做跟hash有关的。注意要引入头文件map. map一般配合pair使用，而且map是默认有序的
8. map.find(tmp.first) != map.end(), 这就说明map里面有tmp.first这个key,就可以直接用map[key]去访问. 便利是这样遍历的
```
	for (auto pair : map) {
		cout << pair.first << ' ' << pair.second << endl;
	}
```

9. set同理，只不过引入的是set头文件
10. vector要用vector头文件，插入用push_back
11. 库函数可以sort，要引入algorithm
12. 15题位运算，看到二进制就要想到bitset
	bitset<32> b(n); 说明是32位的bitset，把n放到里面去。b.count()返回的是bitset里面1的个数。
	位运算&是与，｜是或，^是异或, ~是非， >>右移， << 左移。
13. 16开始动态规划啦
tips: python读一行用 input(),读多行用for line in sys.stdin